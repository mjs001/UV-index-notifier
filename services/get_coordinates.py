
def get_coordinates(url):
  #url is a string
  #need a try except block with a get request through geoapify to get the coordinates.
  #Next get res.json() and convert to a dictionary using json.loads(json_string), then construct a dictionary with lat and lon values plucked from the res dictionary and return that


#if __name__ == "__main__":
  #get_coordinates() #insert fake form_data for testing
